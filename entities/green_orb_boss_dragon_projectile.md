---
title: Green Orb Boss Dragon Projectile
category: entities
---

---

# Green Orb Boss Dragon Projectile

This document details the configuration for the "Green Orb Boss Dragon" projectile entity in Noita, intended for AI-assisted modding.

## Entity Definition

The core entity is a projectile with specific behaviors and visual components.

```xml
<Entity 
  name="$projectile_default" 
   >
  ...
</Entity>
```

## Base Projectile Configuration

Inherits fundamental projectile properties.

### Velocity Component

Controls the projectile's movement physics.

| Attribute      | Value | Description                                    |
|----------------|-------|------------------------------------------------|
| `gravity_y`    | `0`   | No vertical gravity applied.                   |
| `air_friction` | `0`   | No air resistance.                             |
| `mass`         | `0.05`| Low mass, affecting how it interacts with physics. |

## Projectile Component

Defines the projectile's unique characteristics and behaviors.

### Key Attributes

| Attribute                   | Value   | Description                                                              |
|-----------------------------|---------|--------------------------------------------------------------------------|
| `lob_min`, `lob_max`        | `0.8`, `1.0` | Controls the projectile's lobbing behavior (arc).                        |
| `speed_min`, `speed_max`    | `350`   | Sets the projectile's constant speed.                                    |
| `die_on_low_velocity`       | `0`     | Projectile does not die if its velocity drops too low.                   |
| `on_death_explode`          | `1`     | The projectile explodes upon death.                                      |
| `on_lifetime_out_explode`   | `1`     | The projectile explodes when its lifetime expires.                       |
| `explosion_dont_damage_shooter` | `1` | The explosion does not harm the entity that fired it.                    |
| `damage`                    | `0.75`  | Base damage dealt by the projectile.                                     |
| `on_collision_die`          | `1`     | The projectile dies upon colliding with something.                       |
| `lifetime`                  | `100`   | Duration in frames before the projectile expires (and explodes).         |
| `knockback_force`           | `1.0`   | The force applied to entities when hit by the projectile.                |
| `collide_with_tag`          | `player_unit` | The projectile will collide with entities tagged as "player_unit".       |

### Explosion Configuration (`config_explosion`)

Details the effects when the projectile explodes.

| Attribute                 | Value   | Description                                                              |
|---------------------------|---------|--------------------------------------------------------------------------|
| `never_cache`             | `1`     | Prevents caching of this explosion for performance.                      |
| `camera_shake`            | `0`     | No camera shake effect from the explosion.                               |
| `explosion_radius`        | `10`    | The radius of the explosion's effect.                                    |
| `explosion_sprite`        | `data/particles/explosion_016_plasma_green.xml` | The visual sprite used for the explosion.                              |
| `ray_energy`              | `5000`  | Energy value for any ray effects generated by the explosion.             |
| `hole_destroy_liquid`     | `1`     | The explosion can destroy liquids.                                       |
| `hole_enabled`            | `1`     | The explosion creates a hole in surfaces.                                |
| `hole_image`              | `data/temp/explosion_hole.png` | The image used for the explosion hole.                                 |
| `explosion_sprite_emissive` | `1` | The explosion sprite emits light.                                        |
| `explosion_sprite_additive` | `1` | The explosion sprite uses additive blending.                             |
| `damage_mortals`          | `0`     | The explosion does not damage mortal entities directly.                  |
| `physics_explosion_power.min`, `.max` | `0.24`, `0.34` | The minimum and maximum power of the physics-based explosion force.      |
| `physics_throw_enabled`   | `1`     | Physics objects can be thrown by the explosion.                          |
| `shake_vegetation`        | `1`     | The explosion can shake vegetation.                                      |
| `light_r`, `light_g`, `light_b` | `10`, `60`, `10` | RGB values for the light emitted by the explosion.                     |

## Sprite Components

Defines the visual appearance of the projectile.

### Primary Sprite

| Attribute      | Value                               | Description                                    |
|----------------|-------------------------------------|------------------------------------------------|
| `image_file`   | `data/projectiles_gfx/orb_green.xml`| The sprite sheet for the projectile.           |
| `offset_x`, `offset_y` | `6`, `6` | Offset from the entity's origin.               |
| `rect_animation` | `spawn` | Specifies the animation to play.               |
| `update_transform_rotation` | `0` | Sprite does not rotate with the projectile.    |

### Emissive Sprite

A second sprite component, likely for a glowing effect.

| Attribute      | Value                               | Description                                    |
|----------------|-------------------------------------|------------------------------------------------|
| `image_file`   | `data/projectiles_gfx/orb_green.xml`| The sprite sheet for the projectile.           |
| `offset_x`, `offset_y` | `6`, `6` | Offset from the entity's origin.               |
| `rect_animation` | `spawn` | Specifies the animation to play.               |
| `emissive`     | `1`                                 | Sprite is emissive (glows).                    |
| `additive`     | `1`                                 | Sprite uses additive blending.                 |
| `update_transform_rotation` | `0` | Sprite does not rotate with the projectile.    |

## Light Component

Adds a light source to the projectile.

| Attribute | Value | Description                                    |
|-----------|-------|------------------------------------------------|
| `radius`  | `150` | The radius of the light.                       |
| `r`, `g`, `b` | `30`, `90`, `30` | RGB values for the light color.                |

## Variable Storage Component

Stores custom variables for the entity.

| Attribute    | Value                                     | Description                                    |
|--------------|-------------------------------------------|------------------------------------------------|
| `name`       | `projectile_file`                         | The name of the variable.                      |
| `value_string` | `data/entities/projectiles/orb_green_boss_dragon.xml` | The value, referencing its own file path.      |