---
title: Grenade Tier 2 Projectile
category: entities
---

# Grenade Tier 2 Projectile

This document details the configuration of the "grenade_tier_2.xml" entity, representing a player-fired projectile in Noita.

## Core Entity Properties

*   **`name`**: `$projectile_default` (This is a base name, the specific entity is defined by its file path).
*   **`tags`**: `projectile_player` (Identifies this as a projectile originating from the player).

## Base Projectile Configuration

The entity inherits properties from `data/entities/base_projectile.xml`.

### `VelocityComponent`

*   **`mass`**: `0.065` (Determines how the projectile interacts with physics).

## Projectile Component (`ProjectileComponent`)

This is the primary component defining the projectile's behavior.

### Key Attributes:

*   **`lob_min` / `lob_max`**: `0.9` / `1.0` (Controls the arc of the projectile, with `1.0` being a straight trajectory).
*   **`speed_min` / `speed_max`**: `250` / `280` (The initial velocity of the projectile).
*   **`friction`**: `0.6` (How much the projectile's speed is reduced over time due to air resistance).
*   **`direction_random_rad`**: `0.05` (Adds a small amount of randomness to the projectile's initial direction).
*   **`on_death_explode`**: `1` (The projectile will explode when it dies).
*   **`on_lifetime_out_explode`**: `1` (The projectile will explode when its lifetime expires).
*   **`on_collision_die`**: `1` (The projectile will die upon colliding with something).
*   **`lifetime`**: `500` (The maximum duration the projectile exists in frames before expiring).
*   **`lifetime_randomness`**: `7` (Adds a small random variation to the projectile's lifetime).
*   **`damage`**: `1.6` (The base damage dealt by the projectile).
*   **`bounces_left`**: `5` (The number of times the projectile can bounce off surfaces).
*   **`knockback_force`**: `1.0` (The force applied to entities hit by the projectile).
*   **`physics_impulse_coeff`**: `4000` (A multiplier for physics-based impulses, affecting how strongly it pushes objects).
*   **`camera_shake_when_shot`**: `5.0` (The intensity of camera shake when this projectile is fired).
*   **`shoot_light_flash_radius`**: `120` (The radius of the light flash when fired).
*   **`shoot_light_flash_r` / `g` / `b`**: `125` / `255` / `40` (The color of the light flash).

#### `damage_by_type`

*   **`fire`**: `1.2` (Applies an additional 1.2x damage multiplier specifically to fire damage).

#### `config_explosion`

Defines the properties of the explosion that occurs when the projectile dies.

*   **`explosion_radius`**: `25` (The area of effect for the explosion).
*   **`explosion_radius`**: `25` (The area of effect for the explosion).
*   **`explosion_sprite`**: `data/particles/explosion_032_slime.xml` (The visual effect used for the explosion).
*   **`load_this_entity`**: `data/entities/particles/particle_explosion/main_swirly_green.xml` (An entity to spawn upon explosion, likely for more complex visual effects).
*   **`create_cell_probability`**: `40` (The chance, in percent, that the explosion will create new cells/liquids).
*   **`hole_enabled`**: `1` (Enables the creation of holes in terrain by the explosion).
*   **`ray_energy`**: `4500000` (The energy of any magical rays generated by the explosion).
*   **`damage`**: `2.4` (The damage dealt by the explosion itself).
*   **`physics_explosion_power.min` / `max`**: `0.4` / `0.65` (The range of force applied to physics objects by the explosion).
*   **`physics_throw_enabled`**: `1` (Enables physics objects to be thrown by the explosion).
*   **`shake_vegetation`**: `1` (Causes vegetation to shake when the explosion occurs).
*   **`sparks_count_min` / `max`**: `10` / `30` (The number of sparks generated by the explosion).
*   **`spark_material`**: `spark_green` (The type of sparks generated).
*   **`stains_enabled`**: `1` (Enables the creation of stains on surfaces from the explosion).
*   **`stains_radius`**: `12` (The radius of the stains).
*   **`audio_event_name`**: `explosions/magic_grenade_mid` (The sound effect played for the explosion).

## Sprite Component (`SpriteComponent`)

Defines the visual appearance of the projectile.

*   **`image_file`**: `data/projectiles_gfx/grenade_green.xml` (The sprite sheet or definition for the projectile's graphics).
*   **`offset_x` / `offset_y`**: `4` / `4` (Adjusts the position of the sprite relative to the projectile's center).
*   **`additive`**: `1` (Enables additive blending for the sprite, making it appear brighter).

## Particle Emitter Components

These components spawn various particles to enhance the visual feedback.

### Emitter 1 (Smoke)

*   **`emitted_material_name`**: `smoke`
*   **`count_min` / `max`**: `1` / `1`
*   **`lifetime_min` / `max`**: `0.1` / `0.4`
*   **`emission_interval_min_frames` / `max`**: `1` / `3`

### Emitter 2 (Fire)

*   **`emitted_material_name`**: `fire`
*   **`count_min` / `max`**: `1` / `1`
*   **`lifetime_min` / `max`**: `0.1` / `0.4`
*   **`emission_interval_min_frames` / `max`**: `1` / `3`

### Emitter 3 (Green Sparks - Cosmetic)

*   **`emitted_material_name`**: `spark_green`
*   **`count_min` / `max`**: `1` / `10`
*   **`lifetime_min` / `max`**: `1.6` / `3.9`
*   **`gravity.y`**: `20`
*   **`render_on_grid`**: `1`
*   **`fade_based_on_lifetime`**: `1`
*   **`emit_cosmetic_particles`**: `1`

### Emitter 4 (Green Sparks - Trail)

*   **`emitted_material_name`**: `spark_green`
*   **`count_min` / `max`**: `1` / `8`
*   **`gravity.y`**: `-10`
*   **`lifetime_min` / `max`**: `0.5` / `0.8`
*   **`airflow_force`**: `1.2`
*   **`is_trail`**: `1`
*   **`trail_gap`**: `0.1`
*   **`render_on_grid`**: `1`
*   **`fade_based_on_lifetime`**: `1`
*   **`emit_cosmetic_particles`**: `1`

## Audio Component (`AudioComponent`)

*   **`file`**: `data/audio/Desktop/projectiles.bank`
*   **`event_root`**: `player_projectiles/bullet_launcher` (Specifies the sound event to play).

## Light Component (`LightComponent`)

*   **`radius`**: `60`
*   **`r` / `g` / `b`**: `80` / `120` / `10` (A greenish light).

## Variable Storage Component (`VariableStorageComponent`)

*   **`name`**: `projectile_file`
*   **`value_string`**: `data/entities/projectiles/deck/grenade_tier_2.xml` (Stores the path to this entity's file, likely for internal referencing).