---
title: 767aa1e35c2f827b815d45dd9515179b
category: schemas
source: https://github.com/NathanSnail/noitadata/tree/main/data/schemas/767aa1e35c2f827b815d45dd9515179b.xml
---

# 767aa1e35c2f827b815d45dd9515179b

This XML file, identified by the hash `767aa1e35c2f827b815d45dd9515179b`, defines the structure and properties of various game components in Noita. These components are fundamental building blocks that dictate the behavior, appearance, and interactions of entities within the game world, such as enemies, spells, and environmental elements. This schema file acts as a blueprint, specifying the types and sizes of data associated with each component, enabling the game engine to correctly load and interpret entity configurations.

## File Structure

The file is structured as a root `<Schema>` element containing multiple `<Component>` elements. Each `<Component>` element represents a distinct type of game component and is identified by a `component_name` attribute. Inside each `<Component>`, there are `<Var>` elements, which define individual variables or properties associated with that component.

Each `<Var>` element has the following attributes:
*   `name`: The name of the variable.
*   `size`: The size of the variable in bytes.
*   `type`: The data type of the variable (e.g., `int`, `float`, `bool`, `class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >` for strings, `enum RAGDOLL_FX::Enum` for enumerations, `class ceng::math::CVector2<float>` for 2D vectors).

The structure follows a consistent pattern:
```xml
<Schema hash="...">
  <Component component_name="...">
    <Var name="..." size="..." type="...">
    </Var>
    <Var name="..." size="..." type="...">
    </Var>
    ...
  </Component>
  <Component component_name="...">
    ...
  </Component>
  ...
</Schema>
```

## Key Patterns

Several patterns emerge from the sampled content:

*   **Component-Based Design:** The game utilizes a component-based architecture, where entities are composed of various functional components. This file defines the data schema for these components.
*   **Variable Definitions:** Each component's properties are defined as individual variables (`<Var>`) with specific data types and sizes. This allows for granular control over entity behavior.
*   **String Representation:** String data, particularly for names and file paths, is consistently represented using `class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >`.
*   **Numerical Data Types:** Integers (`int`) and floating-point numbers (`float`) are used extensively for numerical properties like distances, durations, speeds, and forces.
*   **Boolean Flags:** Boolean values (`bool`) are used for enabling or disabling specific features or behaviors.
*   **Enumerated Types:** Specific properties, like `ragdoll_fx`, are represented by enumerated types, indicating a fixed set of possible values.
*   **Vector Types:** Complex data like 2D vectors are represented by custom class types, such as `class ceng::math::CVector2<float>`.
*   **Categorization by Component Name:** Components are logically grouped by their `component_name`, suggesting categories like AI behavior (`AIAttackComponent`, `AIComponent`), audio (`AudioListenerComponent`, `AudioLoopComponent`), physics and effects (`PhysicsComponent`, `ParticleEmitterComponent`), and debugging tools (`DebugFollowMouseComponent`, `DebugLogMessagesComponent`).

## Sample Entries

Here are a few representative examples from the sampled content:

1.  **`AIAttackComponent`**: This component appears to define the parameters for an AI's attack capabilities.
    ```xml
    <Component component_name="AIAttackComponent" >
      <Var name="use_probability" size="4" type="int" >
      </Var>
      <Var name="min_distance" size="4" type="float" >
      </Var>
      <Var name="max_distance" size="4" type="float" >
      </Var>
      <Var name="angular_range_deg" size="4" type="float" >
      </Var>
      <Var name="animation_name" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
      </Var>
      <Var name="attack_ranged_entity_file" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
      </Var>
      ...
    </Component>
    ```
    This shows variables for attack probability, range, aiming angles, animation names, and the entity file to spawn for ranged attacks.

2.  **`AudioLoopComponent`**: This component seems to control audio playback, specifically for looping sounds.
    ```xml
    <Component component_name="AudioLoopComponent" >
      <Var name="file" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
      </Var>
      <Var name="event_name" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
      </Var>
      <Var name="auto_play" size="1" type="bool" >
      </Var>
      ...
    </Component>
    ```
    It includes variables for the audio file path, an event name, and a boolean to determine if the sound should play automatically.

3.  **`ParticleEmitterComponent`**: This component likely defines the behavior of particle effects.
    ```xml
    <Component component_name="ParticleEmitterComponent" >
      <Var name="gravity" size="8" type="class ceng::math::CVector2<float>" >
      </Var>
      <Var name="lifetime_min" size="4" type="float" >
      </Var>
      <Var name="lifetime_max" size="4" type="float" >
      </Var>
      <Var name="airflow_force" size="4" type="float" >
      </Var>
      <Var name="count_min" size="4" type="int" >
      </Var>
      <Var name="count_max" size="4" type="int" >
      </Var>
      <Var name="emission_interval_min_frames" size="4" type="int" >
      </Var>
      ...
    </Component>
    ```
    This demonstrates variables for particle gravity, lifetime, airflow effects, emission counts, and emission intervals.

## Reference

This file contains 6404 lines. For complete data, see the [full source](https://github.com/NathanSnail/noitadata/tree/main/data/schemas/767aa1e35c2f827b815d45dd9515179b.xml).

---