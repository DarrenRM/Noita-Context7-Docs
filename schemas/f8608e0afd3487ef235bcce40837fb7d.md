---
title: f8608e0afd3487ef235bcce40837fb7d
category: schemas
source: https://github.com/NathanSnail/noitadata/tree/main/data/schemas/f8608e0afd3487ef235bcce40837fb7d.xml
---

# f8608e0afd3487ef235bcce40837fb7d

This XML file defines the structure and properties of various game components and entities within Noita. It acts as a schema, outlining the data types and names for variables that configure game elements such as AI behaviors, item properties, and physical attributes. This file is crucial for the game's engine to understand and instantiate these elements correctly.

## File Structure

The file is structured as a collection of `<Component>` elements, each representing a distinct type of game component. These components are identified by a `component_name` attribute. Inside each `<Component>`, individual properties are defined using `<Var>` tags.

Each `<Var>` tag has the following attributes:
*   `name`: The name of the variable.
*   `size`: The size of the variable in bytes.
*   `type`: The data type of the variable (e.g., `int`, `float`, `bool`, `class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >`, `struct ValueRange`, `class ceng::math::CVector2<float>`).

The content within a `<Var>` tag is typically empty, as the tag's purpose is to define the variable's metadata rather than its value.

## Key Patterns

*   **Component-Based Design:** The file clearly follows a component-based architecture, where different functionalities (like AI, item handling, physics) are encapsulated in distinct components.
*   **Variable Definitions:** Each component is composed of numerous `<Var>` definitions, specifying the parameters that can be configured for that component.
*   **Data Type Variety:** A range of data types are used, from primitive types like `int`, `float`, and `bool` to more complex types like strings (`class std::basic_string...`), custom structures (`struct ValueRange`), and vector types (`class ceng::math::CVector2<float>`).
*   **Naming Conventions:** Variable names are descriptive, often indicating their purpose (e.g., `use_probability`, `min_distance`, `is_stackable`, `lifetime_max`).
*   **AI Components:** A significant portion of the file appears dedicated to defining various AI behaviors, with components like `AIAttackComponent` and `AnimalAIComponent` detailing how entities perceive, react, and attack.
*   **Item Properties:** Components like `ItemComponent` define fundamental attributes of in-game items, such as whether they are stackable, consumable, or how they behave upon death.
*   **Physics and Movement:** Components related to physics and movement are also present, as seen with variables like `x_vel_min`, `y_vel_max`, and `gravity`.

## Sample Entries

Here are a few representative examples from the sampled content:

**1. `AIAttackComponent`:**
This component defines parameters related to an entity's attack capabilities.

```xml
  <Component component_name="AIAttackComponent" >
    <Var name="use_probability" size="4" type="int" >
    </Var>
    <Var name="min_distance" size="4" type="float" >
    </Var>
    <Var name="max_distance" size="4" type="float" >
    </Var>
    <Var name="angular_range_deg" size="4" type="float" >
    </Var>
    <Var name="animation_name" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
    </Var>
    <Var name="attack_ranged_entity_file" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
    </Var>
    <Var name="attack_ranged_entity_count_min" size="4" type="int" >
    </Var>
    <Var name="attack_ranged_entity_count_max" size="4" type="int" >
    </Var>
  </Component>
```
This sample shows how attack parameters like range, probability, and associated animations or projectile entities are defined.

**2. `AnimalAIComponent`:**
This component seems to define specific AI behaviors for animal-like creatures.

```xml
  <Component component_name="AnimalAIComponent" >
    <Var name="ai_state" size="4" type="int" >
    </Var>
    <Var name="preferred_job" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
    </Var>
    <Var name="escape_if_damaged_probability" size="4" type="int" >
    </Var>
    <Var name="creature_detection_range_x" size="4" type="float" >
    </Var>
    <Var name="creature_detection_range_y" size="4" type="float" >
    </Var>
  </Component>
```
This illustrates how animals might have specific states, preferred actions, and detection parameters.

**3. `ItemComponent`:**
This component outlines the fundamental properties of items in the game.

```xml
  <Component component_name="ItemComponent" >
    <Var name="item_name" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >
    </Var>
    <Var name="is_stackable" size="1" type="bool" >
    </Var>
    <Var name="is_consumable" size="1" type="bool" >
    </Var>
    <Var name="auto_pickup" size="1" type="bool" >
    </Var>
    <Var name="uses_remaining" size="4" type="int" >
    </Var>
    <Var name="remove_on_death" size="1" type="bool" >
    </Var>
  </Component>
```
This shows how basic item attributes like stackability, consumability, and usage counts are defined.

**4. Physics/Movement Variables:**
These variables, seen in the third sample chunk, define physical properties.

```xml
    <Var name="y_pos_offset_max" size="4" type="float" >
    </Var>
    <Var name="area_circle_radius" size="8" type="struct ValueRange" >
    </Var>
    <Var name="x_vel_min" size="4" type="float" >
    </Var>
    <Var name="y_vel_min" size="4" type="float" >
    </Var>
    <Var name="gravity" size="8" type="class ceng::math::CVector2<float>" >
    </Var>
    <Var name="lifetime_min" size="4" type="float" >
    </Var>
    <Var name="lifetime_max" size="4" type="float" >
    </Var>
```
These examples demonstrate how parameters like velocity ranges, gravitational forces, and object lifetimes are specified, likely for projectiles or environmental effects.

## Reference

This file contains 5976 lines. For complete data, see the [full source](https://github.com/NathanSnail/noitadata/tree/main/data/schemas/f8608e0afd3487ef235bcce40837fb7d.xml).

---