---
title: c8ecfb341d22516067569b04563bff9c
category: schemas
source: https://github.com/NathanSnail/noitadata/tree/main/data/schemas/c8ecfb341d22516067569b04563bff9c.xml
---

# c8ecfb341d22516067569b04563bff9c

This XML file defines the structure and properties of various game components used in Noita. It acts as a schema, outlining the types of data and their attributes that can be associated with different game entities and systems. These definitions are crucial for the game engine to understand and instantiate game objects, their behaviors, and their interactions. Essentially, it's a blueprint for how game elements are configured.

## File Structure

The file is structured around a root `<Schema>` element, which contains multiple `<Component>` elements. Each `<Component>` represents a distinct functional unit or behavior within the game.

Within each `<Component>`, there are `<Var>` elements. These `<Var>` elements define individual properties or variables associated with that component. Each `<Var>` has the following key attributes:

*   **`name`**: The identifier for the variable (e.g., `use_probability`, `min_distance`).
*   **`size`**: The size of the data in bytes.
*   **`type`**: The data type of the variable (e.g., `int`, `float`, `bool`, `class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >` which represents a string).

The `<Var>` elements can be empty, indicating that the variable's value is likely managed elsewhere or is a simple flag.

## Key Patterns

*   **Component-Based Architecture**: The game appears to use a component-based system, where entities are composed of various functional components. This file defines the parameters for these components.
*   **AI and Behavior**: A significant portion of the defined components relate to Artificial Intelligence (`AIComponent`, `AIAttackComponent`), detailing how entities perceive, react, and attack.
*   **Visual and Physical Properties**: Components like `DebugSpatialVisualizerComponent` suggest definitions for visual debugging or spatial properties.
*   **Material Interactions**: Components related to material properties and reactions (e.g., temperature, ignition, conversion) are present, indicating a complex simulation of materials.
*   **String Representation**: The `type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >"` indicates that string values, often used for names of animations, entity files, or material arrays, are defined.

## Sample Entries

### AIAttackComponent

This component defines parameters related to an entity's attack capabilities.

```xml
  <Component component_name="AIAttackComponent" >

    <Var name="use_probability" size="4" type="int" >

    </Var>

    <Var name="min_distance" size="4" type="float" >

    </Var>

    <Var name="max_distance" size="4" type="float" >

    </Var>

    <Var name="angular_range_deg" size="4" type="float" >

    </Var>

    <Var name="state_duration_frames" size="4" type="int" >

    </Var>

    <Var name="frames_between" size="4" type="int" >

    </Var>

    <Var name="frames_between_global" size="4" type="int" >

    </Var>

    <Var name="animation_name" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >

    </Var>

    <Var name="attack_ranged_entity_file" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >

    </Var>

    <Var name="attack_ranged_entity_count_min" size="4" type="int" >

    </Var>

    <Var name="attack_ranged_entity_count_max" size="4" type="int" >

    </Var>

  </Component>
```
This sample shows variables controlling attack range, timing, animations, and the properties of spawned projectiles or entities during ranged attacks.

### DebugSpatialVisualizerComponent

This component seems to be for debugging purposes, likely used to visualize spatial areas or bounding boxes.

```xml
  <Component component_name="DebugSpatialVisualizerComponent" >

    <Var name="min_x" size="4" type="float" >

    </Var>

    <Var name="min_y" size="4" type="float" >

    </Var>

    <Var name="max_x" size="4" type="float" >

    </Var>

    <Var name="max_y" size="4" type="float" >

    </Var>

    <Var name="color" size="4" type="unsigned int" >

    </Var>

  </Component>
```
It defines minimum and maximum coordinates (`min_x`, `min_y`, `max_x`, `max_y`) and a `color` attribute, suggesting it's used to draw colored rectangles or areas in the game world for debugging.

### Component related to Material Reactions

This sample shows variables related to how materials react to heat and other conditions.

```xml
    <Var name="fan_the_flames" size="4" type="int" >

    </Var>

    <Var name="temperature_reaction_temp" size="4" type="int" >

    </Var>

    <Var name="ignite_materials" size="4" type="int" >

    </Var>

    <Var name="loop" size="1" type="bool" >

    </Var>

    <Var name="kill_when_finished" size="1" type="bool" >

    </Var>

    <Var name="convert_entities" size="1" type="bool" >

    </Var>

    <Var name="stain_frozen" size="1" type="bool" >

    </Var>

    <Var name="from_material_array" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >

    </Var>

    <Var name="to_material_array" size="24" type="class std::basic_string<char,struct std::char_traits<char>,class std::allocator<char> >" >

    </Var>

    <Var name="mRadius" size="4" type="int" >

    </Var>

  </Component>
```
This snippet reveals parameters for controlling fire spread (`fan_the_flames`), ignition temperatures (`temperature_reaction_temp`), material conversions (`from_material_array`, `to_material_array`), and effects on frozen states (`stain_frozen`).

## Reference

This file contains 7072 lines. For complete data, see the [full source](https://github.com/NathanSnail/noitadata/tree/main/data/schemas/c8ecfb341d22516067569b04563bff9c.xml).

---